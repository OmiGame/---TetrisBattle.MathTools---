---
description: 
globs: 
alwaysApply: true
---
游戏数学建模工具开发规范

####一、命名规范
命名要求：
中文友好的命名方式，清晰地理解类、变量、函数的意义
类名、变量名、函数名 可以用中文。（全英文的文档除外）

如果必须用英文，英文的命名规则参考python的命名规范
类名：大驼峰命名（如表格生成器）
方法名：动词+名词结构（如计算伤害值）
变量名：名词性短语（如暴击概率表）
枚举类型：名词+枚举后缀（如攻击方式枚举）

python
# 示例：战斗模拟模块
class 战斗模拟器:
    def __init__(self, 初始生命值: int, 攻击系数: float):
        self.当前生命值 = 初始生命值
        self.攻击修正系数 = 攻击系数

    def 执行攻击(self, 攻击类型: 攻击方式枚举) -> 战斗结果结构体:
        """执行单次攻击并返回结果"""
        # 实现逻辑...

####二、模块架构设计
功能模块分离，数据、逻辑和表现分离。
**项目结构规范**：
1. 所有核心模块文件应直接放在项目根目录下
2. 避免使用 GenData 和 GenCode 等子目录存放核心模块
3. 数据文件和生成文件可以放在子目录中，但核心逻辑代码应保持在根目录
4. 示例结构：
   项目根目录/
   ├── BattleFormula.py    # 战斗公式模块
   ├── TableData.py        # 表格数据模块
   ├── ExcelGenerater.py   # Excel生成器
   ├── ExcelTools.py       # Excel工具
   ├── Style/              # 样式相关模块
   ├── GenData/            # Luban工具自动生成的数据，禁止把数据和任何模块存放在此文件
   └── GenCode/            # Luban工具自动生成的代码，禁止把数据和任何模块存放在此文件

######三、数据结构设计
在数据类和全局变量中保存基本数据，数据结构简单清晰，必要时使用字典映射。
预定义的字符串类型，优化为枚举类型，不易出错而且可以获得IDE提示。
尽量不要在函数中定义新数据。
每个功能模块的代码需要做到数据和逻辑分离。
期望效果：数据驱动，灵活的数据结构和健壮的函数实现，最低成本修改逻辑处理代码。

四、函数设计规范
1. 健壮性实现
2. 扩展性设计
在  参数可能改变、增减，甚至加入新类型参数的情况下，写健壮的处理函数；

五、防御性编程
可读性强，函数有清晰的功能注释，参数验证、逻辑处理等不同语句之间有明确的分界。
单模块中有清晰的注释划分区域，可读性更好，（比如：用#---------）。
使用语法糖简化代码
在函数内部做好参数的检查，及时发现因输入参数问题导致的bug。
极力避免隐式转换，尤其在函数内部，防止造成难以发现的bug。
在生成数据时，会标明生成数据的日期和相关的参数，方便对比数据，溯源数据信息

六、性能优化
在产生大量数据时，尽量用性能较高的方式处理，避免生成数据的时间过长

七、避免重复
发现代码中大量重复的部分。
减少逻辑处理上的重复，对相同的处理逻辑做进一步的抽象，更通用的抽象。
减少不方便的访问方式带来的重复，可以通过属性定义更便捷的访问方式。

八、注释和文档规范
1. 类注释
每个类都应该有清晰的文档字符串，说明其用途和功能：
```python
class 肉鸽技能数据类:
    """肉鸽技能数据类
    用于存储和管理肉鸽技能的相关数据，包括：
    - 技能名称
    - 属性倍率（生命值、攻击力、攻速等）
    - 持续时间
    - 是否永久存在
    """
```

2. 方法注释
每个方法都应该有详细的文档字符串，包括：
- 方法的功能描述
- 参数说明（类型和用途）
- 返回值说明
- 可能的异常说明
```python
def 计算伤害值(self, 攻击力: float, 防御力: float) -> float:
    """计算单位造成的伤害值
    
    Args:
        攻击力: 攻击方的攻击力数值
        防御力: 防御方的防御力数值
        
    Returns:
        float: 计算得出的伤害值
        
    Raises:
        ValueError: 当攻击力或防御力为负数时抛出
    """
```

3. 模块注释
每个模块文件开头应该有模块级别的文档字符串，说明：
- 模块的主要功能
- 包含的主要类和函数
- 使用示例（如果适用）
```python
"""
战斗信息计算和显示模块

本模块负责计算和格式化战斗过程中的各种统计信息，包括：
- 单位存活数量统计
- 总战力计算
- 战斗结果格式化

主要类：
- BattleInfoCalculator: 战斗信息计算器
"""

from typing import List, Dict, Tuple
```

4. 代码块注释
对于复杂的逻辑处理，应该添加清晰的代码块注释：
```python
# -------------------------------------
# 1. 枚举定义（纯数据）
# -------------------------------------
class 表格类型(Enum):
    角色成长表 = auto()
    士兵表 = auto()
    默认样式 = auto()

# -------------------------------------
# 2. 样式配置类（纯数据结构）
# -------------------------------------
@dataclass
class 字体配置:
    颜色: 颜色风格
    风格: 字体风格 = 字体风格.常规
    大小: int = 11
```

5. 注释风格
- 使用中文注释，保持与代码命名风格一致
- 注释应该简洁明了，避免冗长
- 对于复杂的算法或逻辑，可以添加详细的解释
- 使用统一的注释分隔符（如 # -------------------------------------）
- 对于TODO或FIXME等特殊注释，使用统一的格式：
```python
# TODO: 需要添加对特殊攻击类型的处理
# FIXME: 防御力计算可能存在溢出问题
```

6. 文档维护
- 当修改代码时，同时更新相关的注释
- 定期检查注释的准确性和时效性
- 删除过时或无用的注释
- 保持注释与代码的同步更新
- 当生成新的文件和功能模块时，更新本项目的rules

九、PowerShell终端使用规范
本项目使用PowerShell 5.1作为标准终端环境，所有PowerShell相关的代码和命令必须兼容PowerShell 5.1版本。

1. 版本兼容性要求
- 所有PowerShell脚本和命令必须在PowerShell 5.1中正常运行
- 禁止使用PowerShell 7+独有的语法和功能
- 在编写PowerShell代码时，优先考虑PowerShell 5.1的语法规范

2. 禁用的PowerShell 7+特性
以下PowerShell 7+的特性在本项目中禁止使用：
- `&&` 和 `||` 操作符（使用传统的if-else结构）
- 三元操作符 `? :` （使用if-else结构）
- `ForEach-Object -Parallel` 参数
- `Get-Error` cmdlet（使用 `$Error` 变量）
- 链式比较操作符
- PowerShell 7+的新参数和cmdlet

3. 推荐的PowerShell 5.1兼容写法
```powershell
# 正确：使用传统if-else结构
if ($condition) {
    $result = $value1
} else {
    $result = $value2
}

# 错误：使用PowerShell 7的三元操作符
# $result = $condition ? $value1 : $value2

# 正确：使用传统的逻辑结构
if ($condition1) {
    if ($condition2) {
        # 执行操作
    }
}

# 错误：使用PowerShell 7的链式操作符
# $condition1 && $condition2 && # 执行操作
```

4. 测试要求
- 所有PowerShell脚本在提交前必须在PowerShell 5.1环境中测试
- 确保脚本在Windows PowerShell ISE中能正常运行
- 避免使用可能在PowerShell 5.1中不存在的模块和功能

5. 异常处理
在需要使用PowerShell 7+特性的特殊情况下：
- 必须在代码注释中明确说明原因
- 提供PowerShell 5.1的替代方案
- 确保核心功能在PowerShell 5.1中仍然可用

十、测试代码保护原则
**严格禁止主动删除测试代码**：
1. **绝对保护原则**：
   - 如果用户不主动要求，绝对不要主动删除任何用于测试的相关代码
   - 即使测试代码暂时被注释掉了，也必须完整保留
   - 包括但不限于：`if __name__ == "__main__":` 代码块、调试函数、测试用例、验证代码等

2. **测试代码的重要性**：
   - 测试代码是开发过程中的重要资产，包含了验证逻辑和调试信息
   - 注释掉的测试代码可能在后续开发中重新启用
   - 测试代码有助于理解模块的使用方式和预期行为

3. **允许的操作**：
   - 可以添加新的测试代码
   - 可以修改现有测试代码的内容（如果用户明确要求）
   - 可以注释或取消注释测试代码（如果用户明确要求）
   - 可以重构测试代码的结构（但必须保留原有功能）

4. **禁止的操作**：
   - 禁止删除 `if __name__ == "__main__":` 代码块
   - 禁止删除被注释的测试函数调用
   - 禁止删除调试用的 print 语句（除非用户明确要求）
   - 禁止删除验证和检查代码

5. **代码重构时的处理**：
   - 在进行代码重构、添加region注释、优化结构时，必须确保所有测试代码完整保留
   - 如果需要移动测试代码位置，必须确保功能不受影响
   - 在任何代码修改后，都要验证测试代码是否完整

6. **异常情况**：
   - 只有在用户明确要求删除特定测试代码时，才可以执行删除操作
   - 删除前必须向用户确认具体要删除的代码范围
   - 建议用户将重要的测试代码备份到单独的测试文件中

**记住：测试代码是代码质量保证的重要组成部分，必须像对待生产代码一样谨慎处理！**

